# Release 0.4.0 Prep Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bump `pi-dcp` to `0.4.0`, add a curated release entry, and rewrite `README.md` so it matches the current user-facing feature and config surface.

**Architecture:** This is a docs-and-metadata-only change. Keep the diff limited to `package.json`, `CHANGELOG.md`, and `README.md`, using the current command registrations, config schema, and generated JSON schema as the source of truth.

**Tech Stack:** Node.js package metadata, Markdown docs, Pi slash commands, TypeBox-backed config schema, generated JSON Schema

---

## File Map

- Modify: `package.json` — release version only.
- Modify: `CHANGELOG.md` — add `0.4.0` curated notes above `0.3.0`.
- Modify: `README.md` — usage-first rewrite with full config reference.
- Reference only: `src/commands/register.ts` — authoritative command list.
- Reference only: `src/config-schema.ts` — authoritative config keys/defaults/descriptions.
- Reference only: `dcp.schema.json` — published schema shape to mention and mirror.

---

### Task 1: Bump package version

**Files:**

- Modify: `package.json`

- [ ] **Step 1: Edit the version field to `0.4.0`**

```json
{
  "name": "@pi-vault/pi-dcp",
  "version": "0.4.0"
}
```

- [ ] **Step 2: Verify only the package version changed**

Run: `git diff -- package.json`
Expected: one-line version change from `0.3.0` to `0.4.0`

- [ ] **Step 3: Commit the version bump**

```bash
git add package.json
git commit -m "chore: bump version to 0.4.0"
```

---

### Task 2: Add curated `0.4.0` changelog entry

**Files:**

- Modify: `CHANGELOG.md`
- Reference: `package.json`
- Reference: `src/commands/register.ts`
- Reference: `src/config-schema.ts`
- Reference: `dcp.schema.json`

- [ ] **Step 1: Insert the new release section above `0.3.0`**

```md
## [0.4.0] - 2026-07-08

### Added

- Compression notifications can now include summary text when `compress.showCompression` is enabled.
- New `dcp:permission` command toggles compress tool permission at runtime.
- Deduplication now supports `strategies.deduplication.turnProtection` to keep recent duplicate tool output from being pruned too aggressively.
- Generated `dcp.schema.json` is now shipped with the package for config tooling and validation.

### Changed

- Token counting now uses the Anthropic tokenizer with fallback behavior, improving context and pruning estimates.
- Configuration loading and validation now derive from TypeBox schemas, keeping runtime validation, defaults, and published schema aligned.

### Fixed

- Config validation now resets invalid values more consistently and warns when `maxContextPercent` is not greater than `minContextPercent`.
- Generated JSON Schema no longer emits misleading `required` arrays for optional user config.
```

- [ ] **Step 2: Check the entry against shipped behavior, not commit noise**

Run: `git diff -- CHANGELOG.md`
Expected: one new `0.4.0` section; no edits to older release entries

- [ ] **Step 3: Commit the changelog update**

```bash
git add CHANGELOG.md
git commit -m "docs: add 0.4.0 release notes"
```

---

### Task 3: Rewrite README overview, commands, and workflows

**Files:**

- Modify: `README.md`
- Reference: `src/commands/register.ts`

- [ ] **Step 1: Replace the top half with a usage-first structure**

````md
# @pi-vault/pi-dcp

Keep long Pi sessions usable by pruning stale tool output, reporting what changed, and nudging the model to compress older context before the window fills up.

## Install

```sh
pi install npm:@pi-vault/pi-dcp
```
````

Restart Pi after install.

To try a local checkout before publishing:

```sh
pi -e /absolute/path/to/pi-dcp
```

## Quick Start

```text
dcp:context
dcp:help
dcp:stats
dcp:sweep
```

````

- [ ] **Step 2: Update the commands table to match the current registered commands exactly**

```md
| Command                    | Purpose                                        |
| -------------------------- | ---------------------------------------------- |
| `dcp:help`                 | List all available commands                    |
| `dcp:context`              | Show context usage and DCP state               |
| `dcp:stats`                | Show compression and token savings statistics  |
| `dcp:sweep`                | Force-prune all eligible tool outputs          |
| `dcp:manual on`            | Pause automatic compression                    |
| `dcp:manual off`           | Resume automatic compression                   |
| `dcp:decompress <blockId>` | Deactivate a compression block                 |
| `dcp:recompress <blockId>` | Reactivate a compression block                 |
| `dcp:lifetime`             | Show aggregate statistics across saved sessions|
| `dcp:permission`           | Toggle compress permission between allow/deny  |
````

- [ ] **Step 3: Add practical workflows that cover the new runtime control**

```md
## Typical workflows

**Default:** install it and let DCP prune duplicates and stale errors automatically.

**Need a cleanup pass first?** Run `dcp:sweep`, then `dcp:context`.

**Want manual compression control?** Use `dcp:manual on`, compress selectively, then `dcp:manual off`.

**Need to block compression temporarily?** Run `dcp:permission` to flip between `allow` and `deny`.

**Need to undo a compression block?** Use `dcp:decompress <blockId>` and `dcp:recompress <blockId>`.
```

- [ ] **Step 4: Verify the README command names against source**

Run: `git diff -- README.md && rg "registerCommand\\(\"dcp:" src/commands/register.ts`
Expected: every command documented in `README.md` appears in `src/commands/register.ts`, including `dcp:permission`

- [ ] **Step 5: Commit the README structure rewrite**

```bash
git add README.md
git commit -m "docs: rewrite README for 0.4.0 usage"
```

---

### Task 4: Update README configuration section to match the current schema

**Files:**

- Modify: `README.md`
- Reference: `src/config-schema.ts`
- Reference: `dcp.schema.json`

- [ ] **Step 1: Replace the config example with one that includes the current surface area**

```json
{
  "enabled": true,
  "debug": false,
  "nudgeNotification": "minimal",
  "nudgeNotificationType": "status",
  "protectedFilePatterns": [],
  "compress": {
    "mode": "range",
    "permission": "allow",
    "showCompression": false,
    "maxContextPercent": 80,
    "minContextPercent": 50,
    "maxContextLimit": 200000,
    "minContextLimit": 100000,
    "modelMaxLimits": {},
    "modelMinLimits": {},
    "nudgeFrequency": 5,
    "iterationNudgeThreshold": 15,
    "nudgeForce": "soft",
    "protectedTools": ["compress"],
    "protectUserMessages": false,
    "protectTags": false,
    "summaryBuffer": true
  },
  "manualMode": {
    "default": false,
    "automaticStrategies": true
  },
  "strategies": {
    "deduplication": {
      "enabled": true,
      "protectedTools": [],
      "turnProtection": 0
    },
    "purgeErrors": {
      "enabled": true,
      "turns": 4,
      "protectedTools": []
    }
  },
  "experimental": {
    "allowSubAgents": false,
    "customPrompts": false
  }
}
```

- [ ] **Step 2: Update the option reference for the new and changed fields**

```md
- `compress.permission` — runtime allow/deny gate for the `compress` tool; `dcp:permission` toggles it in-session.
- `compress.showCompression` — when `true`, detailed notifications include the compression summary text.
- `maxContextLimit` / `minContextLimit` — accept either absolute token counts or percentage strings such as `"80%"`.
- `strategies.deduplication.turnProtection` — keeps recent duplicate tool output for N turns before it becomes prune-eligible.
- `dcp.schema.json` — generated schema shipped with the package for editor tooling and config validation workflows.
```

- [ ] **Step 3: Add a short `What's new in 0.4.0` section aligned with the release notes**

```md
## What's new in 0.4.0

- Compression notifications can now surface summary text with `compress.showCompression`.
- `dcp:permission` adds runtime control over compress-tool usage.
- Deduplication can preserve recent duplicates via `turnProtection`.
- Config validation and the shipped `dcp.schema.json` now come from the same TypeBox source of truth.
```

- [ ] **Step 4: Verify the README config keys against schema sources**

Run: `rg -n "showCompression|turnProtection|permission|nudgeNotificationType|dcp.schema.json" README.md src/config-schema.ts dcp.schema.json`
Expected: each documented key exists in both the README and the schema sources

- [ ] **Step 5: Amend the README commit with the config work**

```bash
git add README.md
git commit --amend --no-edit
```

---

### Task 5: Final verification and release-prep cleanup

**Files:**

- Modify if needed: `README.md`
- Modify if needed: `CHANGELOG.md`
- Modify if needed: `package.json`

- [ ] **Step 1: Review the full docs diff for scope control**

Run: `git diff --stat HEAD~3..HEAD`
Expected: changes limited to `package.json`, `CHANGELOG.md`, and `README.md` for release content, plus normal commit metadata

- [ ] **Step 2: Run the narrowest meaningful verification command**

Run: `pnpm run generate:schema`
Expected: completes successfully; no unexpected schema drift beyond already-documented fields

- [ ] **Step 3: If schema generation changes the file unexpectedly, stop and inspect before proceeding**

```bash
git diff -- dcp.schema.json
# Expected: ideally no diff. If there is a diff, confirm README/schema drift before deciding whether to update docs or include the regenerated file.
```

- [ ] **Step 4: Run the release sanity check**

Run: `pnpm run pack:dry-run`
Expected: package packs successfully and includes `src`, `dcp.schema.json`, `CHANGELOG.md`, `LICENSE`, and `README.md`

- [ ] **Step 5: Commit the final release-prep state**

```bash
git add package.json CHANGELOG.md README.md dcp.schema.json
git commit -m "docs: prepare 0.4.0 release"
```

---

## Self-Review

### Spec coverage

- `package.json` bump: covered by Task 1.
- Curated `0.4.0` changelog entry: covered by Task 2.
- Usage-first README rewrite: covered by Task 3.
- Full config reference with current keys and examples: covered by Task 4.
- Narrow verification against current code/schema: covered by Task 5.

No spec gaps found.

### Placeholder scan

- No `TBD`, `TODO`, or “implement later” placeholders.
- Every task has exact file paths, concrete text to add, and explicit commands.
- Verification commands and expected outcomes are included.

### Type and naming consistency

- Command names match `src/commands/register.ts`, including `dcp:permission`.
- Config keys match `src/config-schema.ts` / `dcp.schema.json`: `showCompression`, `turnProtection`, `nudgeNotificationType`, `permission`.
- Release version stays `0.4.0` throughout.
